package test;

import utest.Test;
import utest.Assert;
import lang.Namespace;
import lang.ISeq;

class NamespaceTest extends Test {
	function test() {
		Assert.isTrue(true);
		// trace(">>>>>>>>>>> NamespaceTest");
        // var ns:ISeq = Namespace.all();
		// trace("ALL: " + ns + " " +  ns.count() );
        // while (ns!= null){
        //     var v = ns.first();
        //     trace("::: " + v);
        //     ns = ns.next();
        // }
	}
}
